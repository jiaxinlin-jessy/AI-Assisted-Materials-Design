name: AIMat
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy
  - scipy
  - matplotlib
  - pandas
  - jupyter
  - seaborn
  - scikit-learn
  - pip
  - pip:
      - pymatgen --upgrade
      - mp-api
